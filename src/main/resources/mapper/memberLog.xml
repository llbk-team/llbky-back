<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.member.dao.MemberLogDao">
    <insert id="insert" useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO member_log(member_id, log_type, log_date, created_at, updated_at)
        VALUES (#{memberId}, #{logType}, #{logDate}, now(), now())
    </insert>
</mapper>